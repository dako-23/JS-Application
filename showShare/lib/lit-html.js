import { render, html, nothing } from '../node_modules/lit-html/lit-html.js'

// export const render = (template) => baseRender(template, document.querySelector('#main'))
export {
    html,
    render,
    nothing,
}